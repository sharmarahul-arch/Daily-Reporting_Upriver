{
  "_comment": "Copy to credentials.json and fill in. Do NOT commit the real file.",
  "_comment_sheets": "Leave sheets_service_account_path empty and just drop your Google service-account .json into the config/ folder — the tool auto-detects it. Or set a path here (absolute, or relative to the project root).",
  "amazon_email": "your-seller-central-email@example.com",
  "amazon_password": "your-amazon-password",
  "sheets_service_account_path": "",
  "telegram_bot_token": "OPTIONAL — leave empty if not using the Telegram bot",
  "telegram_chat_id": 0
}
